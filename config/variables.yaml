# == Variables ==
# 
# You can define here your project Variables.
#
Variables:
  msauth:
    tenant:
    # [password]
    # include 'offline_access' in order to get a 'refresh_token'
    scope: user.read openid profile offline_access
